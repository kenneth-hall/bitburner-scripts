# BN4 — The Singularity (planning notes)

Scaffold for BitNode 4. **Not yet entered** — filled in as it becomes relevant. Kept
here so BN1-finish planning that touches "what's next" has a place to land, without
spoiling progression ahead of unlock. Convert relative dates to absolute when logging.

## Status
- **Not started.** Currently finishing BN1 (see [bn1-handoff.md](bn1-handoff.md)).
- Entry requires destroying `w0r1d_d43m0n` in BN1 (the current endgame target).

## Why BN4 matters (the headline)
- BN4 is the Singularity node: completing it grants **SF4**, which unlocks the
  `ns.singularity.*` API (buy augs, join factions, travel, work, install — all scriptable).
- This directly removes the "manual UI only / no Singularity" constraint that shapes
  every current BN1 plan. Much of the manual-lever tooling (aug shop reads via CDP,
  hand-grinding rep, click-by-click installs) becomes automatable **after** SF4.

## Open questions (fill in on entry)
- What are BN4's specific multiplier penalties vs BN1? (money, hacking, rep curves)
- SF4 level effects (SF4.1 vs .2 vs .3 — RAM cost of Singularity fns drops per level).
- Strategy: does the existing daemon/batcher port cleanly, or does the node's economy
  demand a different early game?

## Sources
- `markdown/` API docs (check first), local game files, official Bitburner GitHub.
- Do **not** source-dive to shortcut mechanics; static tables/costs are fine.

## Log
<!-- dated entries as BN4 knowledge accrues -->
